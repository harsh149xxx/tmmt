const HomeButton = ({children}) => {
    return (
        <div className="bg-white text-black w-fit h-fit px-4 py-2 rounded-full font-semibold">
            {children}
        </div>
    )
}

export default HomeButton